<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.hit.hradar.domain.evaluation.query.mapper.EvaluationSectionMapper">
    <select id="selectSectionsByEvalTypeId"
            resultType="org.hit.hradar.domain.evaluation.query.dto.response.EvaluationSectionResponseDto">
        SELECT
            es.section_id          AS sectionId,
            es.section_title       AS sectionTitle,
            es.section_description AS sectionDescription,
            es.section_order       AS sectionOrder
        FROM evaluation_section es
        WHERE es.eval_type_id = #{evalTypeId}
        ORDER BY es.section_order ASC

    </select>

</mapper>
